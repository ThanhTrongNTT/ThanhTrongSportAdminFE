import React from 'react';

const Loading = () => {
    return (
        <div className='h-8 w-8 animate-spin rounded-full border-[3px] border-white border-t-transparent bg-transparent' />
    );
};

export default Loading;
